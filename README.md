# Draft Portfolio

A Vite + React site for showcasing works-in-progress art pieces. Use it as a staging ground to iterate on visual ideas before they are final.

## Getting Started

```bash
npm install
npm run dev
```

The dev server will open at http://localhost:5173.

## Build for Production

```bash
npm run build
```

This outputs static files to `dist/` (ignored in version control).